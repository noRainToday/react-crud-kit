# 启用 shamefully-hoist 来将所有依赖提升到根目录
shamefully-hoist=true

# 启用 hoist-pattern 来控制依赖提升
hoist-pattern[]=*

# 禁用子包的独立 node_modules
node-linker=hoisted

# 启用严格的 peer dependencies
strict-peer-dependencies=false